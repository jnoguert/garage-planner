{
  "name": "garage-planner",
  "version": "1.0.0",
  "description": "Parking exit simulator: checks whether every car can reach the exit.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "python -m http.server 8000"
  }
}
